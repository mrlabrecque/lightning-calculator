import { PlayerNameNumberPipe } from './player-name-number.pipe';

describe('PlayerNameNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerNameNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
